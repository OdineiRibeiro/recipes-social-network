<h1>Editando Receitas</h1>

<ul>
<% @recipe.errors.full_messages.each do |msg| %>
  <li><%= msg %></li>
<% end %>
</ul>

<%= form_for @recipe do |f| %>
  Nome: <br><%= f.text_field :name %><br/>
  Rendimento: <br><%= f.text_field :yield %><br/>
  Tempo de preparo: <br><%= f.text_field :preparation_time %><br/>
  Ingredientes: <br><%= f.text_area :ingredients %><br/>
  Modo de preparo: <br><%= f.text_area :instructions %><br/>
  Foto: <br><%= f.text_field :photo %><br/>
  Cozinha: <br><%= f.collection_select(:cuisine_id, @cuisines, :id, :name, include_blank: true) %></p>
  Dificuldade: <br><%= f.collection_select(:difficulty_id, @difficulties, :id, :name, include_blank: true) %></p>
  Tipo: <br><%= f.collection_select(:description_id, @descriptions, :id, :name, include_blank: true) %></p>
  Preferência: <br><%= f.collection_select(:preference_id, @preferences, :id, :name, include_blank: true) %></p>
  <%= f.submit %>
<% end %>

<%= link_to 'Pagina inicial', action: 'index' %>
<%= link_to 'Editar', action: 'edit' %>
